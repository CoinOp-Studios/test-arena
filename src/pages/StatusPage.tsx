import { Box } from "@chakra-ui/react";
import React, { FC } from "react";

interface IProps {};

const StatusPage:FC<IProps> = (props) => {
  return (
    <Box id="statuspage" className="page">
      Status page
    </Box>
  )
};

export default StatusPage;
